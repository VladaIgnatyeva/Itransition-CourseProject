{
  "name": "site",
  "version": "0.1.0",  
  "scripts": {
    "start": "cd server && npm install && npm run serve",
    "build": "cd client && npm install && npm run build:prod"
  },
  "engines": {
    "node": "10.x"
  }
}
